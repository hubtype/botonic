# IMPORTANT

Copy the contents of the `sample-config` folder for CI tooling rather than this
entire folder. To use the Rspack bundler, copy `rspack.config.ts` from this folder
to the root of your bot project.
