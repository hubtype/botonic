{
  "private": true,
  "devDependencies": {
    "@types/node": "^12.0.3",
    "lerna": "^3.4.0",
    "ts-node": "5",
    "tslib": "^1.9.0",
    "typescript": "^3.5.3"
  },
  "engines": {
    "node": ">=8.0.0"
  },
  "license": "MIT",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "test": "lerna run test",
    "build": "lerna run build",
    "clean": "rimraf packages/**/lib"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "emoji-picker-react": "^2.1.1"
  }
}
